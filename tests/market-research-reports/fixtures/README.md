# Synthetic fixtures

The tests use the seven populated templates in `../../assets/` as valid
synthetic fixtures and mutate in-memory copies to exercise failure cases.

All entities, values, classifications, and `example.invalid` URLs are fictional.
They must not be used as real market evidence.
